{ config, lib, pkgs, ... }:

{
  # Machine-specific configuration for nix-darwin host
  # Common darwin configuration is in ../default.nix

  # Add any machine-specific overrides or configuration here
  environment.systemPackages = [];
}
